import React from 'react'

const MovieDirector = ({director}) => {
    return(
        <div>
            <h4>Director(s)</h4>
            <p>{director}</p>
        </div>
    )
};

export default MovieDirector